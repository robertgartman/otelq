---
name: implement
description: "Context-first implementation workflow: update governing context/ docs, validate acceptance criteria, plan, test, implement, and run quality checks. Use when the user asks to implement or build non-trivial changes governed by a SPEC."
allowed-tools: Bash, Read, Edit, Write, Task
user-invocable: true
---

# Context-First Implementation

First, read the canonical instructions at `.agents/skills/implement/SKILL.md` and follow them exactly.

This Claude Code entry is only a pointer — all guidance is maintained in the canonical file so the two cannot drift.
